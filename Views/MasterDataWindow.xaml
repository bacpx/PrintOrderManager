<Window x:Class="PrintOrderManager.Views.MasterDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Quản Lý Danh Mục" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0" Margin="0,0,0,16" materialDesign:ColorZoneAssist.Mode="PrimaryMid">
            <!-- Tab Chất Liệu -->
            <TabItem Header="Chất Liệu">
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8" HorizontalAlignment="Right">
                        <Button Command="{Binding DeleteMaterialCommand}" CommandParameter="{Binding SelectedItem, ElementName=MaterialsGrid}" Content="Xóa Dòng" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="Red" BorderBrush="Red"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" x:Name="MaterialsGrid"
                              ItemsSource="{Binding Materials}" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="True"
                              CanUserDeleteRows="False"
                              HeadersVisibility="Column"
                              materialDesign:DataGridAssist.CellPadding="8 4">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên Chất Liệu" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Tab Gia Công -->
            <TabItem Header="Gia Công">
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8" HorizontalAlignment="Right">
                        <Button Command="{Binding DeleteProcessCommand}" CommandParameter="{Binding SelectedItem, ElementName=ProcessesGrid}" Content="Xóa Dòng" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="Red" BorderBrush="Red"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" x:Name="ProcessesGrid"
                              ItemsSource="{Binding Processes}" 
                              AutoGenerateColumns="False" 
                              CanUserAddRows="True"
                              CanUserDeleteRows="False"
                              HeadersVisibility="Column"
                              materialDesign:DataGridAssist.CellPadding="8 4">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên Gia Công" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Hủy" Click="BtnCancel_Click" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
            <Button Command="{Binding SaveCommand}" Content="Lưu Thay Đổi" Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
